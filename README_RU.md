# 🖥️ Command System Emulator PDP-11

| 🐧 Linux (Тёмная) | 🪟 Windows (Светлая) | 🍎 macOS (Тёмная) |
| :---: | :---: | :---: |
| ![Linux](assets/Pictures/linux_dark_ru.png) | ![Windows](assets/Pictures/windows_light_ru.png) | ![macOS](assets/Pictures/macos_dark_ru.png) |

![C++17](https://img.shields.io/badge/C++-17-blue.svg)
![Qt6](https://img.shields.io/badge/Qt-6.0+-green.svg)
![Platform](https://img.shields.io/badge/Platform-Linux%20%7C%20Windows%20%7C%20macOS-lightgrey.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

**Command System Emulator PDP-11** — это кроссплатформенный эмулятор классической архитектуры PDP-11 с графическим интерфейсом. Проект создан в образовательных целях для изучения архитектуры ЭВМ, ассемблера и принципов работы процессора.

Эмулятор написан на **C++17** с использованием фреймворка **Qt6**, поддерживает "на лету" дизассемблирование машинного кода, пошаговую отладку и работу с виртуальными внешними устройствами (дисплей, клавиатура, принтер, графический дисплей).

---

## ✨ Ключевые возможности

*   **Интерактивная память и регистры:** Просмотр и редактирование содержимого оперативной памяти и регистров процессора (R0-R7, PSW) в реальном времени.
*   **Встроенный дизассемблер:** Автоматический перевод введенных восьмеричных машинных кодов в мнемоники ассемблера PDP-11 прямо в таблице памяти.
*   **Режимы выполнения:** 
    *   `Step Mode` — пошаговое выполнение для детальной отладки.
    *   `Program Mode` — непрерывное выполнение.
*   **Виртуальные внешние устройства (Memory-Mapped I/O):**
    *   Клавиатура (TKS `177560`, TKB `177562`)
    *   Терминал / Дисплей (TPS `177564`, TPB `177566`)
    *   Принтер (`177514`, `177516`)
    *   Графический дисплей (Координата X `177570`, Координата Y `177572`, Цвет / Триггер отрисовки `177574`)
    *   Аппаратный таймер с генерацией прерываний по вектору `100(8)`.
*   **Графический режим:** Поддержка вывода графики на экран разрешения 64x64 с 16-цветной палитрой (CGA / EGA) посредством процедурных команд ассемблера.
*   **Динамическая тема оформления:** Автоматическая поддержка светлой и темной темы в зависимости от настроек вашей операционной системы (Linux / Windows / macOS).
*   **Справочная система:** Встроенная контекстная справка по командам и прилагаемый подробный справочник в форматах [**PDF**](Docs/PDP11%20RU.pdf) и [**Markdown**](Docs/PDP11%20RU.md).
*   **Двуязычная поддержка:** Интерфейс приложения, встроенный дизассемблер, контекстная справка по командам и подробный справочник автоматически адаптируются под язык вашей системы (русский / английский).
*   **Сохранение и загрузка:** Поддержка импорта и экспорта программ в формате `.pdp`.
*   **Авторизация и защита от плагиата:** Интеграция с университетским порталом Moodle (`edu.vsu.ru`). Система автоматически определяет роль пользователя (студент или преподаватель), динамически применяя соответствующие правила академического контроля. Сохраненные программы `.pdp` привязываются к студенческому билету с помощью криптографической подписи (HMAC-SHA256) и отпечатка оборудования (Hardware Fingerprint), что полностью исключает возможность списывания и сдачи чужих работ.

---

## 🚀 Примеры программ

В папке `Programs/` находятся готовые программы для тестирования эмулятора (подробности в файле [**Program Descriptions RU.txt**](Docs/Program%20Descriptions%20RU.txt)):

1.  **Hello World** — классический вывод строки на экран терминала с использованием цикла и проверки флага готовности устройства.
2.  **Keyboard** — эхо-ввод: считывание символов с клавиатуры и их вывод на экран с выходом по нажатию клавиши 'q'.
3.  **Print** — вывод текста на виртуальное устройство печати.
4.  **Single-digit Adder** — программа для работы с однозначными числами: принимает выражение вида `3+4`, вычисляет сумму и выводит результат.
5.  **16-bit Adder and Subtractor** — работа с многозначными числами: поддерживает сложение и вычитание. Демонстрирует алгоритм перевода бинарного значения в десятичный ASCII-вид для вывода.
6.  **16-bit Integer Calculator** — полноценный калькулятор на 4 действия (+, -, *, /). Включает программную реализацию алгоритмов умножения и деления, которые отсутствуют в базовом наборе инструкций PDP-11.
7.  **Stopwatch** — демонстрация инструкции `WAIT` и работы с аппаратным таймером. Программа отсчитывает реальные секунды в регистре R0, обрабатывая 10 прерываний таймера (по 100 мс каждое) в секунду. Процессор находится в режиме ожидания между тиками.
8.  **Yellow Dot** — простейший тест графического вывода. Последовательно записывает координаты и код цвета в порты дисплея для отрисовки одной желтой точки, после чего останавливается.
9.  **Rainbow Flag** — демонстрация генерации процедурной графики. За счет вложенных циклов по осям X и Y заполняет экран 64x64 градиентной радужной сеткой.
10. **Bouncing Pixel** — физическая симуляция в реальном времени. Рассчитывает столкновения точки с границами экрана, меняет направление движения при соударениях, переключает палитру цветов и использует оптимизацию тактов отрисовки для исключения мерцания.

---

## 🛠️ Сборка и установка

### 🐧 Сборка под Linux (Arch Linux / Manjaro)

1. Установите необходимые зависимости (компилятор, CMake, Qt6):
   ```bash
   sudo pacman -S base-devel cmake qt6-base mesa
   ```
2. Склонируйте репозиторий и запустите скрипт сборки:
   ```bash
   git clone https://github.com/Arta48/PDP11.git
   cd PDP11
   ```

**Вариант А: Портативный запуск**
Быстрая компиляция и запуск без установки в систему:
```bash
sh compile.sh
./build/PDP11
```

**Вариант Б: Системная установка (Рекомендуется)**
Проект включает скрипт для автоматической сборки и установки эмулятора в виде нативного пакета pacman. Скрипт сам создаст ярлык `.desktop`, сгенерирует иконки нужных размеров и пропишет пути.
```bash
sh setup_package.sh
```
*После завершения установки вы сможете запускать эмулятор прямо из меню приложений вашей системы или просто набрав `pdp11` в терминале. Для чистого удаления программы используйте `sudo pacman -R pdp11`.*

### 🍎 Сборка и установка под macOS (macOS 11.0+)

Самый простой способ — скачать готовый универсальный образ **`PDP11.dmg`** напрямую со страницы [**Релизов**](https://github.com/Arta48/PDP11/releases). Он нативно поддерживает как процессоры Apple Silicon (M1 / M2 / M3), так и платформы Intel.

Для ручной сборки из исходных файлов:

1. Установите необходимые зависимости через пакетный менеджер Homebrew (CMake, Qt6):
   ```bash
   brew install cmake qt6
   ```
2. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/Arta48/PDP11.git
   cd PDP11
   ```
3. Сконфигурируйте проект, скомпилируйте его и упакуйте все зависимости Qt во внешний образ `.dmg`:
   ```bash
   # Конфигурация и компиляция
   cmake -B build -DCMAKE_BUILD_TYPE=Release
   cmake --build build -j$(sysctl -n hw.ncpu)

   # Линковка системных dylib-библиотек в бандл и сборка DMG
   macdeployqt build/PDP11.app -dmg
   ```
   *Готовый установщик `PDP11.dmg` будет находиться в директории `build/`.*

### 🪟 Сборка под Windows

Проект включает скрипт для полностью автоматизированной сборки независимого `.exe` файла, который не требует предварительной настройки окружения.

1. Запустите файл `compile.bat` (двойным кликом или через консоль).
2. Скрипт всё сделает за вас:
   * Скачает и установит среду **MSYS2** (в `C:\msys64`).
   * Скачает компилятор GCC, CMake и статическую версию Qt6 со всеми зависимостями.
   * Сконфигурирует и скомпилирует проект.
3. Готовый файл `PDP11.exe` будет лежать в директории `build-windows/`.

*💡 Примечание: Для полной очистки системы от среды сборки после окончания компиляции достаточно просто удалить папку `C:\msys64`.*

### 🐧 Кросс-компиляция для Windows (из-под Linux)

Проект поддерживает сборку статического `.exe` файла для Windows напрямую из Linux через кросс-компиляцию MinGW.

1. Установите MinGW и добавьте репозитории `ownstuff`:
   ```bash
   sudo pacman -S mingw-w64-gcc
   
   if ! grep -q "ownstuff" /etc/pacman.conf; then
       echo -e "
   [ownstuff]
   SigLevel = Optional TrustAll
   Server = https://ftp.f3l.de/~martchus/\$repo/os/\$arch
   Server = https://martchus.dyn.f3l.de/repo/arch/\$repo/os/\$arch" | sudo tee -a /etc/pacman.conf > /dev/null
   fi
   
   sudo pacman-key --keyserver keyserver.ubuntu.com --recv-keys B9E36A7275FC61B464B67907E06FE8F53CDC6A4C
   sudo pacman-key --finger B9E36A7275FC61B464B67907E06FE8F53CDC6A4C
   sudo pacman-key --lsign-key B9E36A7275FC61B464B67907E06FE8F53CDC6A4C
   
   sudo pacman -Syy
   sudo pacman -S mingw-w64-cmake mingw-w64-qt6-base-static
   ```
2. Запустите скрипт сборки для Windows:
   ```bash
   sh compile_windows.sh
   ```
3. Готовый файл `PDP11.exe` будет лежать в директории `build-windows/`.

---

## 📁 Структура проекта

*   `src/` — исходный код на C++ (`Pdp11.cpp` - ядро эмулятора, `MainWindow.cpp` - графический интерфейс).
*   `assets/` — ресурсы графического интерфейса (иконки, шрифты для UI).
*   `Docs/` — документация: справочное руководство и детальное описание логики работы примеров.
*   `Programs/` — коллекция готовых программ (дампы `.pdp`).
*   `CMakeLists.txt` — конфигурация сборочной системы.
